<div class="container">
	<p id="notice"><%= notice %></p>

	<p>
	  <strong>Name:</strong>
	  <%= @quiz.name %>
	  <%= @quiz.category_id %>
	</p>

	<%= link_to 'Edit', edit_quiz_path(@quiz) %> |
	<%= link_to 'Back', quizzes_path %>
</div>
