<div class="se-pre-con"></div>

<div class="container">

	<h1 id="hubtext">Trivia Showdown: Staging</h1>
	<h1 id="hubh2">Where Champions Are Found</h1>

	<div class="jumbotron">

	 <!-- EXAMPLE BUTTON -->
	 <!-- <%= link_to "History Battle", historytrivia_path, class: "btn btn-primary btn-lg" %> --> 

		 	<div id="divCircle">
		 	
		     <div id="middleBubble">
		     	<p id="historytext">History: <br> Countdown Until Next Game (Placeholder)</p> 
		     	<p id="sportstext">Sports: <br>Coming Soon</p> 
		     	<p id="businesstext">Business: <br>Coming Soon</p>
		     	<p id="poptext">Pop Culture: <br>Coming Soon</p>
		     	<p id="randomtext">Random: <br>Coming Soon</p>
		     	<p id="geotext">Geography: <br>Coming Soon</p>
		     	<p id="movietext">Movies <br> and TV: <br>Coming Soon</p>
		     	<p id="sciencetext">Science: <br>Coming Soon</p>
		     </div>
		     <%= image_tag("sportsv1.png", id: "sportsbubble") %> 
		     <%= image_tag("businessv1.png", id: "businessbubble") %> 
		      <%= image_tag("popculturev1.png", id: "popbubble") %> 
		       <%= image_tag("sciencev1.png", id: "sciencebubble") %> 
		     <%= image_tag("randomv1.png", id: "randombubble") %> 
		      <%= image_tag("geov3.png", id: "geobubble") %> 
		       <%= image_tag("moviesv1.png", id: "moviebubble") %> 
		  
		        <%= link_to('historytrivia') do %> <%= image_tag("historyv2.png", id: "historybubble", :data => {bubble1: "example", bubble2: "example2"}) %>

		        <!-- Had class: "homepageGridDefault", to create default bubble but removed --> 
		        <% end %> 

		</div>


	<p id="hubpara">Choose your trivia game category to find games starting soon.</P>

	</div>

</div> 


<script>

$(window).load(function() {
		// Animate loader off screen
		$(".se-pre-con").fadeOut("3500");;
	});


$(document).ready(function(){
     //Center the "info" bubble in the  "circle" div
     var divTop = ($("#divCircle").height() - $("#middleBubble").height())/2;
     var divLeft = ($("#divCircle").width() - $("#middleBubble").width())/2;
     $("#middleBubble").css("top",divTop + "px");
     $("#middleBubble").css("left",divLeft + "px");
}); 

$(document).ready(function(){
     //Arrange the icons in a circle centered in the div
     numItems = $( "#divCircle img" ).length; //How many items are in the circle?
     start = 0.25; //the angle to put the first image at. a number between 0 and 2pi
     step = (2*Math.PI)/numItems; //calculate the amount of space to put between the items.
     
     //Now loop through the buttons and position them in a circle
     $( "#divCircle img" ).each(function( index ) {
          radius = ($("#divCircle").width() - $(this).width())/2; //The radius is the distance from the center of the div to the middle of an icon
          //the following lines are a standard formula for calculating points on a circle. x = cx + r * cos(a); y = cy + r * sin(a)
          //We have made adjustments because the center of the circle is not at (0,0), but rather the top/left coordinates for the center of the div
          //We also adjust for the fact that we need to know the coordinates for the top-left corner of the image, not for the center of the image.
          tmpTop = (($("#divCircle").height()/2) + radius * Math.sin(start)) - ($(this).height()/2);
          tmpLeft = (($("#divCircle").width()/2) + radius * Math.cos(start)) - ($(this).width()/2);
          start += step; //add the "step" number of radians to jump to the next icon
                 
          //set the top/left settings for the image
          $(this).css("top",tmpTop);
          $(this).css("left",tmpLeft);
     });
}); 


$(document).ready(function(){
	// History Info 
	$("#historytext").hide(); 
	$("#historybubble").mouseover(function(){
		$("#historytext").delay('350').fadeIn(); 

	});
	$("#historybubble").mouseout(function() {
		$("#historytext").toggle('slide'); 
		}); 
	
	// Sports Info
	$("#sportstext").hide(); 
	$("#sportsbubble").mouseover(function(){
		$("#sportstext").delay('350').fadeIn(); 

	});

	$("#sportsbubble").mouseout(function() {
		$("#sportstext").toggle('slide'); 
		}); 

	// Business Info
	$("#businesstext").hide(); 
	$("#businessbubble").mouseover(function(){
		$("#businesstext").delay('350').fadeIn(); 

	});

	$("#businessbubble").mouseout(function() {
		$("#businesstext").toggle('slide'); 
		}); 

	// Pop Culture 

	$("#poptext").hide(); 
	$("#popbubble").mouseover(function(){
		$("#poptext").delay('350').fadeIn(); 

	});

	$("#popbubble").mouseout(function() {
		$("#poptext").toggle('slide'); 
		}); 

	// Random

	$("#randomtext").hide(); 
	$("#randombubble").mouseover(function(){
		$("#randomtext").delay('350').fadeIn(); 

	});

	$("#randombubble").mouseout(function() {
		$("#randomtext").toggle('slide'); 
		}); 

	// Geo

	$("#geotext").hide(); 
	$("#geobubble").mouseover(function(){
		$("#geotext").delay('350').fadeIn(); 

	});

	$("#geobubble").mouseout(function() {
		$("#geotext").toggle('slide'); 
		}); 

	// Movie

	$("#movietext").hide(); 
	$("#moviebubble").mouseover(function(){
		$("#movietext").delay('350').fadeIn(); 

	});

	$("#moviebubble").mouseout(function() {
		$("#movietext").toggle('slide'); 
		}); 

	// Science

	$("#sciencetext").hide(); 
	$("#sciencebubble").mouseover(function(){
		$("#sciencetext").delay('350').fadeIn(); 

	});

	$("#sciencebubble").mouseout(function() {
		$("#sciencetext").toggle('slide'); 
		}); 

	// End Loop 
	});


</script> 




	