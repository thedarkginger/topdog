<%= form_for(@chipz) do |f| %>
  <% if @chipz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chipz.errors.count, "error") %> prohibited this chipz from being saved:</h2>

      <ul>
      <% @chipz.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="field">
      <%= f.label :category %><br>
      <%= f.text_field :category, class: "form-control" %>
    </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :rank %><br>
    <%= f.number_field :rank, class: "form-control" %>
  </div>
</div>

<div class="form-group">
  <div class="field">
    <%= f.label :prize %><br>
    <%= f.number_field :prize, class: "form-control" %>
  </div>
</div>

<div class="form-group">

  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
